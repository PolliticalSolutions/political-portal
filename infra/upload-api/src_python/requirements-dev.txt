# Test-only dependencies for the marked-register Python handlers.
# boto3 is provided by the Lambda runtime in production, so it is not in the
# per-function requirements.txt; the unit tests need it to import the handler
# modules (their boto3 clients are constructed at import time). pdf2image /
# pytesseract / Pillow are intentionally omitted — the pure-function tests do
# not need Tesseract, and the handlers guard those imports (OCR_AVAILABLE).
boto3
pytest
